<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body>
  <%- include('navbar'); %>

  <div class="available-container">
    <div class="tickets-available-text">
      <h1>Tickets available on <%=journeyDay%>/<%=journeyMonth%></h1>
    </div>
    <div>
      <%for(let i = 0; i < journeyList.length; i++){%>
      <div class="available-trips">
        <div class="available-destination">
          <h3><%=journeyList[i].departure%>/<%=journeyList[i].arrival%></h3>
        </div>
        <div class="available-departure-time">
          <p>train departure at: <%=journeyList[i].departureTime%></p>
        </div>
        <div class="price-and-button">
          <h1 class="available-price"><%=journeyList[i].price%>$</h1>
          <a href="/panier?journey=<%=journeyList[i]._id%>"><button class="okbutton">OK</button></a>
        </div>
      </div>
      <%}%>
    </div>
  </div>
</body>

</html>